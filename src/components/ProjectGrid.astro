<div class="grid gap-8 md:grid-cols-2">
  <!-- RCS -->
  <div class="bg-white rounded-lg shadow-md p-6 text-left">
    <h2 class="text-xl font-semibold mb-2">Radiology Call Simulator</h2>
    <p class="text-sm text-gray-600 mb-2">Full-stack (Django + Firestore + React)</p>
    <p class="text-gray-800 mb-4">
      A training simulator for radiology residents. Handles case selection, session tracking, audio transcription (WebSpeech API), and image review with DICOM viewer integration.
    </p>
    <p class="text-sm italic text-gray-500 mb-4">⚠️ Code and demo private at client request</p>
    <button
      onclick="document.getElementById('rcs-modal').showModal()"
      class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition"
    >
      View Screenshots
    </button>
  </div>

  <!-- Baseball Almanac (Legacy) -->
  <div class="bg-white rounded-lg shadow-md p-6 text-left">
    <h2 class="text-xl font-semibold mb-2">Baseball Almanac (Legacy)</h2>
    <p class="text-sm text-gray-600 mb-2">Rails + Vue + PostgreSQL</p>
    <p class="text-gray-800 mb-4">
      Historical baseball database exploring players, teams, and league stats from CSVs and scraped data. Featured basic visualizations and searchable tables.
    </p>
    <p class="text-sm italic text-gray-500">🗓 Originally built in 2020 – not currently deployed</p>
  </div>

  <!-- Baseball Almanac v2 -->
  <div class="bg-white rounded-lg shadow-md p-6 text-left border-2 border-dashed border-gray-300">
    <h2 class="text-xl font-semibold mb-2">Baseball Almanac v2 (WIP)</h2>
    <p class="text-sm text-gray-600 mb-2">Planned: Rails + Vue + D3</p>
    <p class="text-gray-800">
      Actively rebuilding from the ground up with better structure, modern code, and improved visualizations. New features will include dynamic stat selectors and improved UX.
    </p>
    <p class="text-sm italic text-gray-500 mt-2">🚧 Coming soon</p>
  </div>
</div>

<!-- RCS Modal -->
<dialog
  id="rcs-modal"
  class="backdrop:bg-black/50 rounded-lg max-w-4xl w-full p-4 overflow-hidden"
  style="position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%);"
>
  <form method="dialog">
    <button class="absolute top-2 right-4 text-2xl text-gray-500 hover:text-black">&times;</button>
  </form>
  <h3 class="text-xl font-semibold mb-4 text-center">Radiology Call Simulator – Screenshots</h3>

  <div class="relative flex items-center justify-center">
    <button
      class="absolute left-0 text-3xl text-gray-500 hover:text-black px-3 z-10"
      onclick="prevImage()"
      aria-label="Previous"
    >
      &#10094;
    </button>

    <img
      id="carousel-image"
      src="/images/rcs-homepage.png"
      alt="RCS screenshot"
      class="rounded shadow border object-contain w-full max-h-[500px]"
    />

    <button
      class="absolute right-0 text-3xl text-gray-500 hover:text-black px-3 z-10"
      onclick="nextImage()"
      aria-label="Next"
    >
      &#10095;
    </button>
  </div>

  <script type="text/javascript" is:inline>
    const screenshots = [
        "/images/rcs-homepage.png",
        "/images/rcs-signup.png",
        "/images/rcs-accountpage.png",
        "/images/rcs-gamepage.png",
        "/images/rcs-reviewpage.png"
    ];
    let index = 0;
    const img = document.getElementById("carousel-image");

    function showImage(i) {
        index = (i + screenshots.length) % screenshots.length;
        img.src = screenshots[index];
    }

    function prevImage() {
        showImage(index - 1);
    }

    function nextImage() {
        showImage(index + 1);
    }

    // expose globally so the buttons can use them
    window.prevImage = prevImage;
    window.nextImage = nextImage;
  </script>
</dialog>